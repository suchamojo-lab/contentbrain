import type { ContentUniverse } from "../../lib/recommendation";
export function UniversePreview({universe,onReveal}:{universe:ContentUniverse;authenticated:boolean;onUnlock:()=>void;onReveal:()=>void}) { const territory=universe.territory.territories[0]; return <main className="universe-preview"><span className="editorial-kicker">YOUR CONTENT UNIVERSE / PREVIEW</span><h1>We found your<br/><em>Content Universe.</em></h1><div className="preview-counts"><div><strong>{universe.territory.territories.length}</strong><span>Content Territories</span></div><div><strong>{universe.contentPillars.length}</strong><span>Content Pillars</span></div><div><strong>{universe.storyBank.length}</strong><span>Grounded Stories</span></div><div><strong>{universe.ideaUniverse.reduce((sum,group)=>sum+group.ideas.length,0)}</strong><span>Idea Directions</span></div></div><article className="preview-world"><span>YOUR ARCHETYPE</span><h2>{universe.territory.archetype}</h2><p>{universe.territory.positioning}</p><h3>First territory</h3><strong>{territory?.title}</strong><p>{territory?.description}</p></article><div className="locked-universe"><div/><div/><div/><p>Your full Content Universe is ready.</p></div><button className="button button--ink" onClick={onReveal}>REVEAL MY FULL UNIVERSE →</button><small>No account needed to see your result.</small></main>; }
