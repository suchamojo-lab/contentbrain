import type {ShareableUniverse} from "./shareableUniverse";
export function wrapShareCardText(text:string,maxWidth:number,measure:(value:string)=>number){const words=text.trim().split(/\s+/);const output:string[]=[];let line="";for(const word of words){const candidate=line?`${line} ${word}`:word;if(line&&measure(candidate)>maxWidth){output.push(line);line=word}else line=candidate}if(line)output.push(line);return output}
function drawLines(ctx:CanvasRenderingContext2D,text:string,x:number,y:number,maxWidth:number,lineHeight:number,maxLines=5){const wrapped=wrapShareCardText(text,maxWidth,(value)=>ctx.measureText(value).width).slice(0,maxLines);wrapped.forEach((line,index)=>ctx.fillText(line,x,y+index*lineHeight));return y+wrapped.length*lineHeight}
export function renderShareCard(canvas:HTMLCanvasElement,identity:ShareableUniverse,domain:string){canvas.width=1080;canvas.height=1350;const ctx=canvas.getContext("2d");if(!ctx)throw new Error("Share card canvas is unavailable.");ctx.fillStyle="#f5f1e7";ctx.fillRect(0,0,1080,1350);ctx.strokeStyle="rgba(17,19,21,.12)";ctx.lineWidth=2;for(let i=0;i<5;i++){ctx.beginPath();ctx.arc(875,215,65+i*34,0,Math.PI*2);ctx.stroke()}ctx.fillStyle="#dfff45";ctx.beginPath();ctx.arc(875,215,78,0,Math.PI*2);ctx.fill();ctx.fillStyle="#17191b";ctx.font="600 24px Arial, sans-serif";ctx.fillText("EVERYTHING CONTENT",70,82);ctx.font="600 21px Arial, sans-serif";ctx.fillText("MY CONTENT UNIVERSE",70,178);ctx.font="72px Georgia, serif";let y=drawLines(ctx,identity.archetype.toUpperCase(),70,270,850,76,3)+20;ctx.fillStyle="#5f681d";ctx.font="600 18px Arial, sans-serif";ctx.fillText("MY STRONGEST TERRITORY",70,y);y+=58;ctx.fillStyle="#17191b";ctx.font="42px Georgia, serif";y=drawLines(ctx,identity.territory.join("  ×  "),70,y,900,52,4)+40;ctx.fillStyle="#5f681d";ctx.font="600 18px Arial, sans-serif";ctx.fillText("YOU COULD BECOME KNOWN FOR",70,y);y+=60;ctx.fillStyle="#17191b";ctx.font="46px Georgia, serif";drawLines(ctx,`“${identity.positioning}”`,70,y,920,56,5);ctx.fillStyle="#dfff45";ctx.fillRect(0,1190,1080,160);ctx.fillStyle="#17191b";ctx.font="600 19px Arial, sans-serif";ctx.fillText("BUILT WITH CONTENT BRAIN",70,1250);ctx.font="38px Georgia, serif";ctx.fillText("WHAT’S YOUR CONTENT UNIVERSE?",70,1303);ctx.font="600 18px Arial, sans-serif";ctx.textAlign="right";ctx.fillText(domain,1010,1301);ctx.textAlign="left"}
export async function downloadShareCard(canvas:HTMLCanvasElement,filename="my-content-universe.png"){const blob=await new Promise<Blob>((resolve,reject)=>canvas.toBlob((value)=>value?resolve(value):reject(new Error("Could not create the share card.")),"image/png"));const url=URL.createObjectURL(blob);const link=document.createElement("a");link.href=url;link.download=filename;link.click();URL.revokeObjectURL(url)}
