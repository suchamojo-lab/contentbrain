import type { ContentUniverse } from "./recommendation";
const section=(title:string,items:string[])=>`\n## ${title}\n${items.map((item)=>`- ${item}`).join("\n")}`;
export function toLlmContext(universe:ContentUniverse) { return `# MY CONTENT UNIVERSE V0.1\n\n## Character — AI-inferred\n${universe.character.summary}${section("Identity signals",universe.character.identitySignals)}\n\n## Natural gifts\n${universe.gifts.superpower}${section("Strengths",universe.gifts.naturalStrengths)}\n\n## Radiance${section("Topics",universe.radiance.topics)}${section("Intersections",universe.radiance.interestingIntersections.map((item)=>`${item.title}: ${item.insight}`))}\n\n## Expression\n${universe.expression.profile}${section("Best formats",universe.expression.bestFormats)}\n\n## Territory\n${universe.territory.archetype}\n${universe.territory.positioning}${section("Territories",universe.territory.territories.map((item)=>`${item.title}: ${item.description}`))}${section("Content pillars",universe.contentPillars.map((pillar)=>`${pillar.title}: ${pillar.why}`))}${section("Stories — source is user-stated; lesson and hook are AI-inferred",universe.storyBank.map((story)=>`${story.title} | Source: ${story.source} | Lesson: ${story.lesson} | Hook: ${story.potentialHook}`))}${section("Idea directions",universe.ideaUniverse.flatMap((group)=>group.ideas.map((idea)=>`[${group.pillar}] ${idea}`)))}\n\n## Honesty rule\nNever turn an inference into a fact. Never invent proof, numbers, achievements, clients, credentials, or experiences.`; }
